<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Career Paths</title>
    <!-- Bootstrap 5.3 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">


    <style>
        body {
            background-color: rgb(187, 182, 182);
            margin: 0;
            padding: 0;
        }

        .navbar {
            background-color: #223141 !important;
        }

        #logo_1 {
            font-family: 'Oswald', sans-serif;
            text-decoration: none;
            color: white;
            font-size: 25px;
            font-weight: bold;
        }

        #logo_2 {
            font-size: 25px;
            font-weight: bold;
            color: #0a6fe9;
            font-family: 'Oswald', sans-serif;
        }

        .nav-item {
            display: inline-block;
            margin-left: 10px;
            margin-right: 15px;
            font-size: 16px !important;
        }

        .nav-link {
            font-family: 'Oswald', sans-serif;
            text-decoration: none;
            color: white;
            transition: all 0.3s linear;
        }

        .nav-link:hover {
            color: red !important;
        }

        #active {
            color: #D35400 !important;
        }

        .nav-link {
            font-family: 'Open Sans', sans-serif;
            font-weight: 700;
        }

        #links {
            color: white;
        }

        #links:hover {
            background-color: #D35400;
        }


        .container1 {
            margin-left: 30px !important;
            margin-right: 10px !important;
        }

        /* Card Container Styling */
        .row {
            margin-top: 20px;
        }

        /* Card Styling */
        .career-card {
            min-height: 40px;
            background-color: #f8f9fa;
            border: 1px solid #ddd;
            border-radius: 8px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card-class {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
        }

        /* Hover Effect */
        .career-card:hover {
            transform: translateY(-5px);
            box-shadow: 0px 8px 16px rgba(0, 0, 0, 0.2);
        }

        /* Card Content Styling */
        .card-title {
            font-size: 1.25rem;
            font-weight: bold;
            color: #343a40;
            margin-bottom: 0.5rem;
        }

        .card-text {
            font-size: 0.9rem;
            color: #555;
            line-height: 1.4;
            margin-bottom: 1rem;
        }

        /* "See More" Link Styling */
        .toggle-link {
            font-size: 0.9rem;
            color: #007bff;
            cursor: pointer;
            text-decoration: none;
        }

        .toggle-link:hover {
            text-decoration: underline;
        }

        /* Card Link Styling */
        .card-link {
            display: inline-block;
            color: #17a2b8;
            font-weight: 600;
            text-decoration: none;
        }

        .card-link:hover {
            color: #138496;
            text-decoration: underline;
        }

        /* Spacing between Cards */
        .col {
            padding: 15px;
        }

        h1 {
            font-family: 'Oswald', sans-serif;
            margin: 10px;
            margin-bottom: 15px;
            font-weight: bold;


        }

        .carrer-button {
            text-decoration: none;
            background-color: #5b5b70;
            border-radius: 5px;
            color: white;
            padding: 5px;
            border: 1px solid black;
            font-size: 13px;
            transition: all 0.3s linear;
        }

        .card-body {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 15px;
        }

        .carrer-button:hover {
            background-color: rgb(186, 77, 77);
            border-color: white;
        }
    </style>

</head>

<body>

    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid ">

            <a class="navbar-brand" href="#">
                <span id="logo_1">Career</span>
                <span id="logo_2">Path</span>

            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText"
                aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse  navbar-collapse" id="navbarText">

                <ul class="navbar-nav ms-auto  mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link " aria-current="page" href="/api/users/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/api/users/contact">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/api/users/about">About</a>
                    </li>
                    <% if (isAuthenticated) { %>
                        <% if (role==='admin' ) { %>
                            <!-- Admin specific links -->
                            <li class="nav-item"><a class="nav-link" href="/api/users/admin">Add Course</a></li>
                            <!-- <li class="nav-item"><a class="nav-link" href="/api/users/admin/msg">Msg</a></li> -->
                            <li class="nav-item"><a class="nav-link" href="/api/users/logout">SignOut</a></li>
                            <% } else { %>
                                <!-- User specific links -->
                                <li class="nav-item"><a class="nav-link" href="/api/users/logout">SignOut</a></li>
                                <% } %>
                                    <% } else { %>
                                        <li class="nav-item"><a class="nav-link" href="/api/users/signup">SignUp</a>
                                        </li>
                                        <li class="nav-item"><a class="nav-link" href="/api/users/login">SignIn</a></li>
                                        <% } %>
                </ul>

            </div>
        </div>
    </nav>

    <section>
        <div class="container1 mt-3">
            <h1>Career Paths</h1>
            <div class=" card-class">
                <% careerPaths.forEach((career, index)=> { %>
                    <div class="">
                        <div class="card career-card" style="width: 18rem">
                            <div class="card-body">
                                <h5 class="card-title">
                                    <%= career.name %>
                                </h5>

                                <a class="carrer-button" href="/api/users/career/<%= career._id %>"
                                    class="card-link">View Courses</a>

                            </div>
                        </div>
                    </div>
                    <% }) %>
            </div>

            <script>
                function toggleDescription(index) {
                    const moreText = document.querySelector(`#description-${index} .more-text`);
                    const link = document.querySelector(`#description-${index} .toggle-link`);

                    if (moreText.style.display === "none") {
                        moreText.style.display = "inline";
                        link.textContent = "See Less";
                    } else {
                        moreText.style.display = "none";
                        link.textContent = "See More";
                    }
                }
            </script>


            <script>
                function toggleDescription(index) {
                    const moreText = document.querySelector(`#description-${index} .more-text`);
                    const link = document.querySelector(`#description-${index} a`);

                    if (moreText.style.display === "none") {
                        moreText.style.display = "inline";
                        link.textContent = "See Less";
                    } else {
                        moreText.style.display = "none";
                        link.textContent = "See More";
                    }
                }
            </script>

        </div>
    </section>
    <!-- Bootstrap 5.3 JS (Popper and Bootstrap JS) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
</body>

</html>